{
  "name": "moin-kark",
  "private": true,
  "version": "1.0.0",
  "description": "Was ist los in Dithmarschen — Kirchenkreis-Event-Karte (Web + iOS + Android)",
  "workspaces": [
    "packages/*",
    "apps/api",
    "apps/app"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspace apps/api && npm run typecheck --workspace apps/app",
    "test": "npm test --workspace packages/shared && npm test --workspace apps/api && npm test --workspace apps/app",
    "typecheck:api": "npm run typecheck --workspace apps/api",
    "typecheck:app": "npm run typecheck --workspace apps/app",
    "dev:api": "npm run dev --workspace apps/api",
    "sync:theme-css": "npm run sync:theme-css --workspace packages/shared",
    "audit:prod": "npm audit --omit=dev"
  },
  "overrides": {
    "nanoid": "^3.3.18",
    "uuid": "^11.1.1"
  }
}
